<!DOCTYPE html>
<html>
<head>
  <title>Voting Settings</title>
  <link rel="stylesheet" href="/css/style.css">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <style>
    .settings-card {
      background: #fff;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
      max-width: 500px;
      margin: 50px auto;
      text-align: center;
    }

    .settings-card h2 {
      margin-bottom: 20px;
    }

    .status-text {
      font-size: 18px;
      margin-bottom: 25px;
    }

    .btn {
      padding: 10px 20px;
      background-color: #007bff;
      border: none;
      color: #fff;
      font-weight: bold;
      border-radius: 6px;
      cursor: pointer;
      transition: 0.3s;
    }

    .btn:hover {
      background-color: #0056b3;
    }

    .btn.red {
      background-color: #dc3545;
    }

    .btn.red:hover {
      background-color: #b02a37;
    }
  </style>
</head>
<body>
  <%- include('../partials/adminHeader') %>

  <div class="settings-card">
    <h2>Voting Settings</h2>
    <p class="status-text">
      Voting is currently:
      <strong style="color:<%= setting.voting_open ? 'green' : 'red' %>">
        <%= setting.voting_open ? 'OPEN' : 'CLOSED' %>
      </strong>
    </p>

    <form action="/admin/toggle-voting" method="POST">
      <button type="submit" class="btn <%= setting.voting_open ? 'red' : '' %>">
        <%= setting.voting_open ? 'End Voting' : 'Start Voting' %>
      </button>
    </form>
  </div>
</body>
</html>
